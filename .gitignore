# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Personal local config (see header comment in either file)
infra/prod-aws/terraform.tfvars
infra/dev-gcp/terraform.tfvars

# google-github-actions/auth writes these federation-config files into the
# workspace during CI — belt-and-suspenders so they can never be committed.
gha-creds-*.json
